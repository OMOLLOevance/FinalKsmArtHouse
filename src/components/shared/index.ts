// =============================================================================\n// SHARED COMPONENTS INDEX - KSM.ART HOUSE\n// Professional component library for maximum reusability\n// =============================================================================\n\n// UI Foundation Components\nexport { Button } from '../ui/Button';\nexport { Card, CardContent, CardHeader, CardTitle, CardDescription, CardFooter } from '../ui/Card';\nexport { Input } from '../ui/Input';\nexport { Badge } from '../ui/Badge';\nexport { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from '../ui/Dialog';\nexport { Select, SelectContent, SelectItem, SelectTrigger, SelectValue } from '../ui/Select';\nexport { LoadingSpinner } from '../ui/LoadingSpinner';\nexport { StatusBadge } from '../ui/StatusBadge';\nexport { SuccessDialog } from '../ui/SuccessDialog';\n\n// Business Logic Components\nexport { StaffSelector } from './StaffSelector';\nexport { MetricCard, calculateRevenueShare } from './DashboardComponents';\nexport { DataTable } from './DataTable';\nexport { FormField } from './FormField';\nexport { StatusCard } from './StatusCard';\n\n// Performance Optimized Components\nexport { MemoizedCard, MemoizedTable, MemoizedTableRow } from '../ui/MemoizedComponents';\nexport { \n  LazyCustomerManager, \n  LazyCateringManager, \n  LazyQuotationManager, \n  LazyEventCategoryManager, \n  LazyEventPaymentForm,\n  LazyWrapper \n} from '../ui/LazyComponents';\n\n// Layout Components\nexport { Sidebar } from '../layout/Sidebar';\n\n// Authentication Components\nexport { AuthGuard } from '../auth/AuthGuard';\nexport { LoginForm } from '../features/auth/LoginForm';\n\n// =============================================================================\n// COMPONENT USAGE GUIDELINES\n// =============================================================================\n\n/**\n * REUSABILITY BEST PRACTICES:\n * \n * 1. DATA COMPONENTS:\n *    - Use DataTable for all list/table displays\n *    - Use StatusCard for status-based information\n *    - Use MetricCard for dashboard statistics\n * \n * 2. FORM COMPONENTS:\n *    - Use FormField for all form inputs\n *    - Use StaffSelector for user filtering\n *    - Use SuccessDialog for completion feedback\n * \n * 3. PERFORMANCE:\n *    - Use Lazy components for heavy features\n *    - Use Memoized components for frequently re-rendered items\n *    - Use LazyWrapper for code splitting\n * \n * 4. CONSISTENCY:\n *    - All components follow the same design system\n *    - TypeScript interfaces ensure type safety\n *    - Accessibility attributes are built-in\n */\n\n// =============================================================================\n// EXAMPLE USAGE\n// =============================================================================\n\n/**\n * // Professional Data Table\n * <DataTable\n *   title=\"Customer Management\"\n *   data={customers}\n *   columns={[\n *     { key: 'name', label: 'Name', sortable: true },\n *     { key: 'email', label: 'Email', sortable: true },\n *     { key: 'status', label: 'Status', render: (value) => <StatusBadge status={value} /> }\n *   ]}\n *   onAdd={() => setShowForm(true)}\n *   searchable\n * />\n * \n * // Reusable Form Field\n * <FormField\n *   type=\"text\"\n *   name=\"customerName\"\n *   label=\"Customer Name\"\n *   value={formData.customerName}\n *   onChange={(value) => setFormData({...formData, customerName: value})}\n *   required\n *   error={errors.customerName}\n * />\n * \n * // Status Display\n * <StatusCard\n *   title=\"Event Planning\"\n *   subtitle=\"Wedding Reception\"\n *   status=\"active\"\n *   amount={150000}\n *   progress={75}\n *   metadata={[\n *     { label: 'Event Date', value: '2024-06-15' },\n *     { label: 'Guests', value: '120' }\n *   ]}\n *   actions={[\n *     { label: 'Edit', onClick: handleEdit },\n *     { label: 'Complete', onClick: handleComplete, variant: 'default' }\n *   ]}\n * />\n */